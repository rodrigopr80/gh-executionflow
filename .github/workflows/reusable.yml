name: Reusable Deploy
on:
  workflow_call:
    inputs:
      artifact-name:
        required: false
        default: dist
        type: string
        description: 'Name of the artifact to download'
    outputs:
      result:
        description: 'Result of the deployment step'
        value: ${{ jobs.deploy.outputs.outcome }}
    # secrets:
    #   some-secret:
    #     required: true
    #     description: 'A secret required for deployment'
jobs:
    deploy:
        outputs:
          outcome: ${{ steps.set-output.outputs.step-result }}
        runs-on: ubuntu-latest
        steps:
          - name: Get build artifacts
            uses: actions/download-artifact@v4
            with:
              name: ${{ inputs.artifact-name }}            
          - name: Output information
            run: echo "Deploying & uploading"
          - name: Set result output
            id: set-output
            run: echo "step-result=success" >> $GITHUB_OUTPUT




    # inputs:
    #   deploy-environment:
    #     required: true
    #     type: string
    #   deploy-token:
    #     required: true
    #     type: string
    # secrets:
    #   DEPLOY_TOKEN:
    #     required: true



        #   - name: Output contents
        #     run: ls
        #   - name: Deploy
        #     run: |
        #       echo "Deploying to ${{ inputs.deploy-environment }}"
        #       # Use the deploy token to authenticate and deploy the application
        #       # Example command (replace with your actual deployment command):
        #       # deploy-command --token ${{ secrets.DEPLOY_TOKEN }} --env ${{ inputs.deploy-environment }}